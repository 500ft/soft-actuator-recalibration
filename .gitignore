.DS_Store
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
tmp/
output/
# Phase D dataset arrays are large and deterministically regenerable
# (python -m scripts.phaseD_dataset); integrity is pinned by manifest SHA-256.
data/sim/phaseD/*.npz

# personal correspondence — keep out of the public repo
outreach/

# rendered candidate PDFs and manifests; the archived docs/preprint_v1.pdf is the only committed PDF
build/
